export enum EOracleKey {
  chainlinkV3 = "chainlinkV3",
}

export enum EQueryParamsKey {
  market = "market",
  isVerified = "isVerified",
  sortBy = "sortBy",
  sortDirection = "sortDirection",
}

export enum ESortDirection {
  default = "default",
  descending = "descending",
  ascending = "ascending",
}
